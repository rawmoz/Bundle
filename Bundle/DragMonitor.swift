import Foundation

// v0.2 — Detects spacebar keypress during an active system drag.
// Requires Accessibility permission (prompted on first use).
// Wired into AppDelegate once implemented.
final class DragMonitor {
    // stub
}
